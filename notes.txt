change the env of both frontend and backend and add the deployed url for socket 
run the deployed site and check if the "User connected with ${id}" appear on backend console


socket used in
    adminDashboard.tsx
    completedRafflesPage.tsx
    app.js.tsx